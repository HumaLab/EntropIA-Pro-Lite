# Commits that only reformat code. git blame skips them when configured with:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
# GitHub reads this file automatically.

# style: apply prettier to the frontend and store sources
bda32ed961f8b32f9be591a26b0efe526322ba31
